#include "../pipex.h"

void	command(char argv[2], char **envp)
{
	//does the specified commands somehow...
	//using split maybe? or idk
}

char	*find_path(char *cmd, char **envp)
{
	//somehow finds the path to the commands we need?
}